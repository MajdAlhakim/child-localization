=== STRUCTURE ===
.
./.agent
./.agent/knowledge
./.agent/knowledge/child-localization-core.md
./.agent/rules
./.agent/rules/child-localization.md
./.env
./.git
./.gitignore
./.pytest_cache
./.pytest_cache/.gitignore
./.pytest_cache/CACHEDIR.TAG
./.pytest_cache/README.md
./.pytest_cache/v
./.pytest_cache/v/cache
./.pytest_cache/v/cache/lastfailed
./.pytest_cache/v/cache/nodeids
./DEPLOYMENT.md
./PRD.md
./__pycache__
./__pycache__/conftest.cpython-313-pytest-9.0.2.pyc
./app
./app/lib
./app/lib/models
./app/lib/models/position_update.dart
./app/lib/screens
./app/lib/screens/live_map_screen.dart
./app/lib/services
./app/lib/services/auth_service.dart
./app/lib/services/websocket_service.dart
./backend
./backend/Dockerfile
./backend/__init__.py
./backend/__pycache__
./backend/__pycache__/__init__.cpython-313.pyc
./backend/app
./backend/app/__init__.py
./backend/app/__pycache__
./backend/app/__pycache__/__init__.cpython-313.pyc
./backend/app/__pycache__/main.cpython-313.pyc
./backend/app/api
./backend/app/api/__init__.py
./backend/app/api/__pycache__
./backend/app/api/__pycache__/__init__.cpython-313.pyc
./backend/app/api/__pycache__/admin.cpython-313.pyc
./backend/app/api/__pycache__/parent.cpython-313.pyc
./backend/app/api/admin.py
./backend/app/api/gateway.py
./backend/app/api/parent.py
./backend/app/api/websocket.py
./backend/app/core
./backend/app/core/__init__.py
./backend/app/core/__pycache__
./backend/app/core/__pycache__/__init__.cpython-313.pyc
./backend/app/core/__pycache__/config.cpython-313.pyc
./backend/app/core/__pycache__/security.cpython-313.pyc
./backend/app/core/ble_parser.py
./backend/app/core/config.py
./backend/app/core/security.py
./backend/app/db
./backend/app/db/__init__.py
./backend/app/db/__pycache__
./backend/app/db/__pycache__/__init__.cpython-313.pyc
./backend/app/db/__pycache__/init_db.cpython-313.pyc
./backend/app/db/__pycache__/models.cpython-313.pyc
./backend/app/db/__pycache__/session.cpython-313.pyc
./backend/app/db/init_db.py
./backend/app/db/models.py
./backend/app/db/session.py
./backend/app/fusion
./backend/app/fusion/__init__.py
./backend/app/fusion/__pycache__
./backend/app/fusion/__pycache__/__init__.cpython-313.pyc
./backend/app/fusion/__pycache__/offset_corrector.cpython-313.pyc
./backend/app/fusion/bayesian_grid.py
./backend/app/fusion/coordinator.py
./backend/app/fusion/ekf.py
./backend/app/fusion/grid_loader.py
./backend/app/fusion/observation_model.py
./backend/app/fusion/offset_corrector.py
./backend/app/fusion/pdr.py
./backend/app/main.py
./backend/app/schemas
./backend/app/schemas/__init__.py
./backend/app/schemas/gateway.py
./backend/app/schemas/position.py
./backend/requirements.txt
./conftest.py
./docker-compose.yml
./firmware
./firmware/bw16
./firmware/bw16/ble_transmitter.ino
./firmware/bw16/imu_reader.ino
./firmware/bw16/packet_format.h
./firmware/bw16/rtt_ranger.ino
./progress.txt
./pytest.ini
./tasks.json
./test-repo.sh
./test-repo.txt
./tests
./tests/__pycache__
./tests/__pycache__/test_admin_endpoints.cpython-313-pytest-9.0.2.pyc
./tests/__pycache__/test_db_schema.cpython-313-pytest-9.0.2.pyc
./tests/__pycache__/test_health.cpython-313-pytest-9.0.2.pyc
./tests/__pycache__/test_offset_corrector.cpython-313-pytest-9.0.2.pyc
./tests/__pycache__/test_security.cpython-313-pytest-9.0.2.pyc
./tests/integration
./tests/integration/test_failure_modes.py
./tests/integration/test_full_pipeline.py
./tests/test_admin_endpoints.py
./tests/test_bayesian_grid.py
./tests/test_ble_parser.py
./tests/test_db_schema.py
./tests/test_ekf.py
./tests/test_fusion_coordinator.py
./tests/test_gateway_endpoints.py
./tests/test_grid_loader.py
./tests/test_health.py
./tests/test_observation_model.py
./tests/test_offset_corrector.py
./tests/test_parent_api.py
./tests/test_pdr.py
./tests/test_security.py
./tests/test_websocket.py

=== tasks.json ===
{
  "project": "Child Indoor Localization System",
  "version": "1.0",
  "description": "Real-time child indoor localization — QU H07 C Corridor senior design project",
  "update_protocol": "pull-merge-push — always git pull before modifying this file. Only modify entries where owner == your identity.",
  "tasks": [
    {
      "id": "TASK-01",
      "title": "Initialize project structure",
      "phase": 1,
      "phase_name": "Scaffolding",
      "owner": "shared",
      "status": "done",
      "completed_at": "2026-02-28T02:45:00Z",
      "completed_by": "shared",
      "notes": "Full scaffold committed and pushed to GitHub; all dirs, placeholder files, configs, and rules in place",
      "acceptance": "git log shows initial commit; all directories exist; tasks.json and progress.txt present",
      "test_command": null
    },
    {
      "id": "TASK-02",
      "title": "requirements.txt and Dockerfile",
      "phase": 1,
      "phase_name": "Scaffolding",
      "owner": "person-a",
      "status": "done",
      "completed_at": "2026-02-28T03:14:00Z",
      "completed_by": "person-a",
      "notes": "requirements.txt and Dockerfile created; docker compose build exits 0",
      "acceptance": "docker compose build completes without errors",
      "test_command": "docker compose build"
    },
    {
      "id": "TASK-03",
      "title": "PostgreSQL schema and SQLAlchemy models",
      "phase": 1,
      "phase_name": "Scaffolding",
      "owner": "person-a",
      "status": "done",
      "completed_at": "2026-02-28T03:39:00Z",
      "completed_by": "person-a",
      "notes": "7 SQLAlchemy ORM tables; SQLite in-memory tests; 14 tests pass",
      "acceptance": "pytest tests/test_db_schema.py passes",
      "test_command": "pytest tests/test_db_schema.py --tb=short -q"
    },
    {
      "id": "TASK-04",
      "title": "Configuration and security core",
      "phase": 1,
      "phase_name": "Scaffolding",
      "owner": "person-a",
      "status": "done",
      "completed_at": "2026-02-28T03:39:00Z",
      "completed_by": "person-a",
      "notes": "pydantic-settings config, JWT via python-jose, bcrypt passwords; 11 tests pass",
      "acceptance": "pytest tests/test_security.py passes",
      "test_command": "pytest tests/test_security.py --tb=short -q"
    },
    {
      "id": "TASK-05",
      "title": "BLE packet parser",
      "phase": 2,
      "phase_name": "Protocol",
      "owner": "person-b",
      "status": "pending",
      "completed_at": null,
      "completed_by": null,
      "notes": null,
      "acceptance": "pytest tests/test_ble_parser.py passes",
      "test_command": "pytest tests/test_ble_parser.py --tb=short -q"
    },
    {
      "id": "TASK-05B",
      "title": "BW16 firmware — IMU reader port to BW16 + MPU6050",
      "phase": 2,
      "phase_name": "Protocol",
      "owner": "person-b",
      "status": "pending",
      "completed_at": null,
      "completed_by": null,
      "notes": null,
      "acceptance": "imu_reader.ino compiles for BW16 (Realtek Arduino SDK); serial output matches packet_format.h Type 0x01 byte layout; verified by manual inspection of packed bytes against packet spec",
      "test_command": "arduino-cli compile --fqbn realtek:AmebaD:rtl8720dn firmware/bw16/imu_reader.ino"
    },
    {
      "id": "TASK-06",
      "title": "Gateway API endpoints",
      "phase": 2,
      "phase_name": "Protocol",
      "owner": "person-b",
      "status": "pending",
      "completed_at": null,
      "completed_by": null,
      "notes": null,
      "acceptance": "pytest tests/test_gateway_endpoints.py passes",
      "test_command": "pytest tests/test_gateway_endpoints.py --tb=short -q"
    },
    {
      "id": "TASK-07",
      "title": "Offset correction engine",
      "phase": 3,
      "phase_name": "Calibration",
      "owner": "person-a",
      "status": "done",
      "completed_at": "2026-02-28T03:39:00Z",
      "completed_by": "person-a",
      "notes": "correct(d_raw, offset) = max(d_raw-offset, 0.5); 7 tests pass",
      "acceptance": "pytest tests/test_offset_corrector.py passes",
      "test_command": "pytest tests/test_offset_corrector.py --tb=short -q"
    },
    {
      "id": "TASK-08",
      "title": "Calibration admin endpoints",
      "phase": 3,
      "phase_name": "Calibration",
      "owner": "person-a",
      "status": "done",
      "completed_at": "2026-02-28T03:39:00Z",
      "completed_by": "person-a",
      "notes": "JWT-protected CRUD for ap_calibration; is_reliable gate (std>20 or n<30); 8 tests pass",
      "acceptance": "pytest tests/test_admin_endpoints.py passes",
      "test_command": "pytest tests/test_admin_endpoints.py --tb=short -q"
    },
    {
      "id": "TASK-09",
      "title": "Floor plan grid loader",
      "phase": 4,
      "phase_name": "Bayesian Grid",
      "owner": "person-c",
      "status": "pending",
      "completed_at": null,
      "completed_by": null,
      "notes": null,
      "acceptance": "pytest tests/test_grid_loader.py passes",
      "test_command": "pytest tests/test_grid_loader.py --tb=short -q"
    },
    {
      "id": "TASK-10",
      "title": "Observation model implementation",
      "phase": 4,
      "phase_name": "Bayesian Grid",
      "owner": "person-c",
      "status": "pending",
      "completed_at": null,
      "completed_by": null,
      "notes": null,
      "acceptance": "pytest tests/test_observation_model.py passes",
      "test_command": "pytest tests/test_observation_model.py --tb=short -q"
    },
    {
      "id": "TASK-11",
      "title": "Bayesian grid update engine",
      "phase": 4,
      "phase_name": "Bayesian Grid",
      "owner": "person-c",
      "status": "pending",
      "completed_at": null,
      "completed_by": null,
      "notes": null,
      "acceptance": "pytest tests/test_bayesian_grid.py passes",
      "test_command": "pytest tests/test_bayesian_grid.py --tb=short -q"
    },
    {
      "id": "TASK-12",
      "title": "EKF predict and update steps",
      "phase": 5,
      "phase_name": "EKF Fusion",
      "owner": "person-c",
      "status": "pending",
      "completed_at": null,
      "completed_by": null,
      "notes": null,
      "acceptance": "pytest tests/test_ekf.py passes",
      "test_command": "pytest tests/test_ekf.py --tb=short -q"
    },
    {
      "id": "TASK-12B",
      "title": "PDR Python module — full MATLAB algorithm migration",
      "phase": 5,
      "phase_name": "EKF Fusion",
      "owner": "person-c",
      "status": "pending",
      "completed_at": null,
      "completed_by": null,
      "notes": null,
      "acceptance": "pytest tests/test_pdr.py passes all tests including: EMA filter convergence, gyro bias calibration, step detection true positive, stationary suppression, near-gravity suppression, debounce, Weinberg formula correctness, histogram bin edges match MATLAB output, SVR feature extraction, stride clamping, position update math",
      "test_command": "pytest tests/test_pdr.py --tb=short -q"
    },
    {
      "id": "TASK-13",
      "title": "Fusion coordinator async loop",
      "phase": 5,
      "phase_name": "EKF Fusion",
      "owner": "person-c",
      "status": "pending",
      "completed_at": null,
      "completed_by": null,
      "notes": null,
      "acceptance": "pytest tests/test_fusion_coordinator.py passes",
      "test_command": "pytest tests/test_fusion_coordinator.py --tb=short -q"
    },
    {
      "id": "TASK-14",
      "title": "WebSocket broadcaster",
      "phase": 6,
      "phase_name": "Real-time API",
      "owner": "person-d",
      "status": "pending",
      "completed_at": null,
      "completed_by": null,
      "notes": null,
      "acceptance": "pytest tests/test_websocket.py passes",
      "test_command": "pytest tests/test_websocket.py --tb=short -q"
    },
    {
      "id": "TASK-15",
      "title": "Parent REST API endpoints",
      "phase": 6,
      "phase_name": "Real-time API",
      "owner": "person-d",
      "status": "pending",
      "completed_at": null,
      "completed_by": null,
      "notes": null,
      "acceptance": "pytest tests/test_parent_api.py passes",
      "test_command": "pytest tests/test_parent_api.py --tb=short -q"
    },
    {
      "id": "TASK-16",
      "title": "Flutter project setup and WebSocket service",
      "phase": 7,
      "phase_name": "Mobile App",
      "owner": "person-d",
      "status": "pending",
      "completed_at": null,
      "completed_by": null,
      "notes": null,
      "acceptance": "flutter analyze shows 0 errors; WebSocket service connects to mock server",
      "test_command": "flutter analyze"
    },
    {
      "id": "TASK-17",
      "title": "Flutter live map screen",
      "phase": 7,
      "phase_name": "Mobile App",
      "owner": "person-d",
      "status": "pending",
      "completed_at": null,
      "completed_by": null,
      "notes": null,
      "acceptance": "flutter build apk --debug succeeds; marker renders at correct coordinates",
      "test_command": "flutter build apk --debug"
    },
    {
      "id": "TASK-18",
      "title": "Full integration test pipeline",
      "phase": 8,
      "phase_name": "Integration",
      "owner": "person-b",
      "status": "pending",
      "completed_at": null,
      "completed_by": null,
      "notes": null,
      "acceptance": "pytest tests/integration/ passes; RMS error printed; latency P95 <= 2.5s",
      "test_command": "pytest tests/integration/ --tb=short -q"
    },
    {
      "id": "TASK-19",
      "title": "Health check and monitoring endpoint",
      "phase": 8,
      "phase_name": "Integration",
      "owner": "person-a",
      "status": "done",
      "completed_at": "2026-02-28T03:39:00Z",
      "completed_by": "person-a",
      "notes": "GET /api/v1/health returns {status,database,fusion_engine,active_devices,uptime_seconds}; 6 tests pass",
      "acceptance": "pytest tests/test_health.py passes",
      "test_command": "pytest tests/test_health.py --tb=short -q"
    },
    {
      "id": "TASK-20",
      "title": "Final validation and cleanup",
      "phase": 8,
      "phase_name": "Integration",
      "owner": "person-d",
      "status": "pending",
      "completed_at": null,
      "completed_by": null,
      "notes": null,
      "acceptance": "pytest --tb=short -q shows 0 failures; docker compose up healthy; DEPLOYMENT.md written; final commit pushed",
      "test_command": "pytest --tb=short -q"
    }
  ]
}
=== progress.txt ===
# Child Indoor Localization System — Session Log
# Format: DONE [TASK-ID] (owner): <description> — <timestamp>
# Append only. Never edit existing lines.
DONE [TASK-01] (shared): Full scaffold � all dirs, placeholders, configs, rules, and docs committed and pushed � 2026-02-28T02:45:00Z
DONE [TASK-02] (person-a): requirements.txt and Dockerfile created; docker compose build exits 0 � 2026-02-28T03:14:00Z
DONE [TASK-03] (person-a): 7 SQLAlchemy ORM tables; SQLite in-memory tests; 14 tests pass � 2026-02-28T03:39:00Z
DONE [TASK-04] (person-a): pydantic-settings config, JWT via python-jose, bcrypt passwords; 11 tests pass � 2026-02-28T03:39:00Z
DONE [TASK-07] (person-a): correct(d_raw, offset)=max(d_raw-offset,0.5); 7 tests pass � 2026-02-28T03:39:00Z
DONE [TASK-08] (person-a): JWT-protected CRUD ap_calibration, is_reliable gate; 8 tests pass � 2026-02-28T03:39:00Z

=== PERSON-A FILES ===
--- backend/app/db/models.py ---
﻿"""SQLAlchemy 2.0 ORM models — PRD Section 13."""
import uuid
from datetime import datetime, timezone

from sqlalchemy import (
    Boolean, Column, Float, ForeignKey, Integer, SmallInteger,
    String, BigInteger, text,
)
from sqlalchemy.dialects.postgresql import UUID as PG_UUID
from sqlalchemy.orm import DeclarativeBase, relationship
from sqlalchemy import DateTime


def _uuid_col(primary_key: bool = False) -> Column:
    """UUID column that works with both PostgreSQL and SQLite (for tests)."""
    return Column(
        PG_UUID(as_uuid=True),
        primary_key=primary_key,
        default=uuid.uuid4,
        server_default=text("gen_random_uuid()") if not primary_key else None,
        nullable=False,
    )


def _now_col() -> Column:
    return Column(DateTime(timezone=True), nullable=False, default=lambda: datetime.now(timezone.utc))


class Base(DeclarativeBase):
    pass


class Device(Base):
    __tablename__ = "devices"

    device_id = Column(PG_UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    mac_address = Column(String(17), unique=True, nullable=False)
    label = Column(String(100), nullable=True)
    created_at = _now_col()
    updated_at = _now_col()
    is_active = Column(Boolean, nullable=False, default=True)

    links = relationship("DeviceLink", back_populates="device", cascade="all, delete")
    imu_samples = relationship("ImuSample", back_populates="device", cascade="all, delete")
    rtt_measurements = relationship("RttMeasurement", back_populates="device", cascade="all, delete")
    position_estimates = relationship("PositionEstimate", back_populates="device", cascade="all, delete")


class ParentUser(Base):
    """Parent user table — implemented by person-d (TASK-15), defined here for FK."""
    __tablename__ = "parent_users"

    user_id = Column(PG_UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    email = Column(String(255), unique=True, nullable=False)
    hashed_password = Column(String(255), nullable=False)
    created_at = _now_col()

    links = relationship("DeviceLink", back_populates="parent_user", cascade="all, delete")


class DeviceLink(Base):
    __tablename__ = "device_links"

    link_id = Column(PG_UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    device_id = Column(
        PG_UUID(as_uuid=True),
        ForeignKey("devices.device_id", ondelete="CASCADE"),
        nullable=False,
    )
    parent_user_id = Column(
        PG_UUID(as_uuid=True),
        ForeignKey("parent_users.user_id", ondelete="CASCADE"),
        nullable=False,
    )
    linked_at = _now_col()

    device = relationship("Device", back_populates="links")
    parent_user = relationship("ParentUser", back_populates="links")


class AccessPoint(Base):
    __tablename__ = "access_points"

    ap_id = Column(PG_UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    bssid = Column(String(17), unique=True, nullable=False)
    ssid = Column(String(100), nullable=True)
    x_m = Column(Float, nullable=True)   # OQ-03: pending IT confirmation
    y_m = Column(Float, nullable=True)
    z_m = Column(Float, nullable=True)
    band = Column(String(10), nullable=True)  # "2.4GHz" or "5GHz"
    created_at = _now_col()

    calibration_entries = relationship("ApCalibration", back_populates="access_point", cascade="all, delete")
    rtt_measurements = relationship("RttMeasurement", back_populates="access_point", cascade="all, delete")


class ApCalibration(Base):
    __tablename__ = "ap_calibration"

    cal_id = Column(PG_UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    ap_id = Column(
        PG_UUID(as_uuid=True),
        ForeignKey("access_points.ap_id", ondelete="CASCADE"),
        nullable=False,
    )
    band = Column(String(10), nullable=False)
    offset_m = Column(Float, nullable=False)
    std_dev_m = Column(Float, nullable=False)
    sample_count = Column(Integer, nullable=False)
    calibrated_at = _now_col()
    is_reliable = Column(Boolean, nullable=False, default=True)

    access_point = relationship("AccessPoint", back_populates="calibration_entries")


class ImuSample(Base):
    __tablename__ = "imu_samples"

    sample_id = Column(PG_UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    device_id = Column(
        PG_UUID(as_uuid=True),
        ForeignKey("devices.device_id", ondelete="CASCADE"),
        nullable=False,
    )
    ts_device_ms = Column(BigInteger, nullable=False)
    ts_server = _now_col()
    ax_ms2 = Column(Float, nullable=False)
    ay_ms2 = Column(Float, nullable=False)
    az_ms2 = Column(Float, nullable=False)
    gx_rads = Column(Float, nullable=False)
    gy_rads = Column(Float, nullable=False)
    gz_rads = Column(Float, nullable=False)
    seq = Column(SmallInteger, nullable=False)

    device = relationship("Device", back_populates="imu_samples")


class RttMeasurement(Base):
    __tablename__ = "rtt_measurements"

    meas_id = Column(PG_UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    device_id = Column(
        PG_UUID(as_uuid=True),
        ForeignKey("devices.device_id", ondelete="CASCADE"),
        nullable=False,
    )
    ap_id = Column(
        PG_UUID(as_uuid=True),
        ForeignKey("access_points.ap_id", ondelete="CASCADE"),
        nullable=False,
    )
    ts_device_ms = Column(BigInteger, nullable=False)
    ts_server = _now_col()
    d_raw_mean_m = Column(Float, nullable=False)
    d_raw_std_m = Column(Float, nullable=False)
    d_corrected_m = Column(Float, nullable=True)
    rssi_dbm = Column(SmallInteger, nullable=False)
    band = Column(String(10), nullable=False)

    device = relationship("Device", back_populates="rtt_measurements")
    access_point = relationship("AccessPoint", back_populates="rtt_measurements")


class PositionEstimate(Base):
    __tablename__ = "position_estimates"

    pos_id = Column(PG_UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    device_id = Column(
        PG_UUID(as_uuid=True),
        ForeignKey("devices.device_id", ondelete="CASCADE"),
        nullable=False,
    )
    ts_server = _now_col()
    x_m = Column(Float, nullable=False)
    y_m = Column(Float, nullable=False)
    source = Column(String(20), nullable=False)   # "fused"|"wifi_only"|"imu_only"
    confidence = Column(Float, nullable=False)
    active_aps = Column(SmallInteger, nullable=False)
    mode = Column(String(20), nullable=False)

    device = relationship("Device", back_populates="position_estimates")

--- backend/app/db/session.py ---
﻿"""Async SQLAlchemy session factory."""
from sqlalchemy.ext.asyncio import create_async_engine, async_sessionmaker, AsyncSession

from backend.app.core.config import settings

engine = create_async_engine(
    settings.DATABASE_URL,
    echo=False,
    future=True,
)

AsyncSessionLocal: async_sessionmaker[AsyncSession] = async_sessionmaker(
    engine,
    expire_on_commit=False,
)


async def get_db() -> AsyncSession:
    """FastAPI dependency — yields an async DB session."""
    async with AsyncSessionLocal() as session:
        yield session

--- backend/app/db/init_db.py ---
﻿"""Database initialisation — called on application startup."""
from sqlalchemy.ext.asyncio import AsyncEngine

from backend.app.db.models import Base
from backend.app.db.session import engine


async def init_db(bind: AsyncEngine | None = None) -> None:
    """Create all tables if they do not exist.

    Args:
        bind: optional engine override (used in tests to pass in-memory engine).
    """
    target = bind or engine
    async with target.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)


async def drop_db(bind: AsyncEngine | None = None) -> None:
    """Drop all tables (test teardown only)."""
    target = bind or engine
    async with target.begin() as conn:
        await conn.run_sync(Base.metadata.drop_all)

--- backend/app/core/config.py ---
﻿"""Application settings via pydantic-settings — reads from .env file."""
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    DATABASE_URL: str = "postgresql+asyncpg://admin:changeme@db:5432/localization"
    SECRET_KEY: str = "dev-secret-key-change-in-production"
    GATEWAY_API_KEY: str = "dev-gateway-key-change-in-production"
    ALLOWED_ORIGINS: str = "http://localhost:3000"
    ACCESS_TOKEN_EXPIRE_MINUTES: int = 30
    ALGORITHM: str = "HS256"

    model_config = SettingsConfigDict(
        env_file=".env",
        env_file_encoding="utf-8",
        extra="ignore",
    )


settings = Settings()

--- backend/app/core/security.py ---
﻿"""JWT authentication and gateway API key verification."""
from datetime import datetime, timedelta, timezone
from typing import Any

from fastapi import HTTPException, status
from jose import JWTError, jwt
import bcrypt as _bcrypt

from backend.app.core.config import settings


# ── Password helpers (using bcrypt directly — passlib incompatible with Python 3.13) ──

def get_password_hash(password: str) -> str:
    return _bcrypt.hashpw(password.encode(), _bcrypt.gensalt()).decode()


def verify_password(plain: str, hashed: str) -> bool:
    return _bcrypt.checkpw(plain.encode(), hashed.encode())


# ── JWT helpers ───────────────────────────────────────────────────────────────

def create_access_token(
    data: dict[str, Any],
    expires_delta: timedelta | None = None,
) -> str:
    """Return a signed JWT. Default expiry: ACCESS_TOKEN_EXPIRE_MINUTES."""
    to_encode = data.copy()
    expire = datetime.now(timezone.utc) + (
        expires_delta if expires_delta is not None
        else timedelta(minutes=settings.ACCESS_TOKEN_EXPIRE_MINUTES)
    )
    to_encode["exp"] = expire
    return jwt.encode(to_encode, settings.SECRET_KEY, algorithm=settings.ALGORITHM)


def verify_token(token: str) -> dict[str, Any]:
    """Decode and validate a JWT. Raises 401 on any failure."""
    credentials_exception = HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Could not validate credentials",
        headers={"WWW-Authenticate": "Bearer"},
    )
    try:
        payload = jwt.decode(token, settings.SECRET_KEY, algorithms=[settings.ALGORITHM])
        return payload
    except JWTError:
        raise credentials_exception


# ── Gateway API key ───────────────────────────────────────────────────────────

def verify_gateway_key(key: str) -> None:
    """Raise 401 if the provided key does not match GATEWAY_API_KEY."""
    if key != settings.GATEWAY_API_KEY:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Invalid gateway API key",
        )

--- backend/app/api/admin.py ---
﻿"""Admin API — calibration data management. Protected by JWT Bearer auth.

PRD §10.4:
    POST   /api/v1/admin/calibration
    GET    /api/v1/admin/calibration
    GET    /api/v1/admin/calibration/{bssid}
    DELETE /api/v1/admin/calibration/{bssid}

Reliability rule (PRD §10.3):
    is_reliable = False  if std_dev_m > 20 OR sample_count < 30
"""
from fastapi import APIRouter, Depends, HTTPException, status
from fastapi.security import HTTPAuthorizationCredentials, HTTPBearer
from pydantic import BaseModel
from sqlalchemy import select, delete
from sqlalchemy.ext.asyncio import AsyncSession

from backend.app.core.security import verify_token
from backend.app.db.models import AccessPoint, ApCalibration
from backend.app.db.session import get_db

router = APIRouter(prefix="/api/v1/admin", tags=["admin"])
_bearer = HTTPBearer()


async def _require_auth(
    creds: HTTPAuthorizationCredentials = Depends(_bearer),
) -> dict:
    return verify_token(creds.credentials)


# ── Request / Response schemas ────────────────────────────────────────────────

class CalibrationIn(BaseModel):
    bssid: str
    band: str          # "2.4GHz" or "5GHz"
    offset_m: float
    std_dev_m: float
    sample_count: int


class CalibrationOut(BaseModel):
    bssid: str
    band: str
    offset_m: float
    std_dev_m: float
    sample_count: int
    is_reliable: bool

    class Config:
        from_attributes = True


def _is_reliable(std_dev_m: float, sample_count: int) -> bool:
    """PRD §10.3 reliability gate."""
    return std_dev_m <= 20.0 and sample_count >= 30


# ── Endpoints ─────────────────────────────────────────────────────────────────

@router.post("/calibration", status_code=status.HTTP_201_CREATED, response_model=CalibrationOut)
async def create_calibration(
    body: CalibrationIn,
    db: AsyncSession = Depends(get_db),
    _auth: dict = Depends(_require_auth),
):
    """Create or update calibration entry for an AP+band pair."""
    # Upsert access_point record
    result = await db.execute(select(AccessPoint).where(AccessPoint.bssid == body.bssid))
    ap = result.scalars().first()
    if ap is None:
        ap = AccessPoint(bssid=body.bssid)
        db.add(ap)
        await db.flush()

    # Check for existing calibration entry for this AP+band
    result = await db.execute(
        select(ApCalibration).where(
            ApCalibration.ap_id == ap.ap_id,
            ApCalibration.band == body.band,
        )
    )
    cal = result.scalars().first()
    if cal is None:
        cal = ApCalibration(ap_id=ap.ap_id)
        db.add(cal)

    cal.band = body.band
    cal.offset_m = body.offset_m
    cal.std_dev_m = body.std_dev_m
    cal.sample_count = body.sample_count
    cal.is_reliable = _is_reliable(body.std_dev_m, body.sample_count)

    await db.commit()
    await db.refresh(cal)

    return CalibrationOut(
        bssid=body.bssid,
        band=cal.band,
        offset_m=cal.offset_m,
        std_dev_m=cal.std_dev_m,
        sample_count=cal.sample_count,
        is_reliable=cal.is_reliable,
    )


@router.get("/calibration", response_model=list[CalibrationOut])
async def list_calibration(
    db: AsyncSession = Depends(get_db),
    _auth: dict = Depends(_require_auth),
):
    result = await db.execute(
        select(ApCalibration, AccessPoint.bssid)
        .join(AccessPoint, ApCalibration.ap_id == AccessPoint.ap_id)
    )
    rows = result.all()
    return [
        CalibrationOut(
            bssid=bssid,
            band=cal.band,
            offset_m=cal.offset_m,
            std_dev_m=cal.std_dev_m,
            sample_count=cal.sample_count,
            is_reliable=cal.is_reliable,
        )
        for cal, bssid in rows
    ]


@router.get("/calibration/{bssid}", response_model=CalibrationOut)
async def get_calibration(
    bssid: str,
    db: AsyncSession = Depends(get_db),
    _auth: dict = Depends(_require_auth),
):
    result = await db.execute(
        select(ApCalibration, AccessPoint.bssid)
        .join(AccessPoint, ApCalibration.ap_id == AccessPoint.ap_id)
        .where(AccessPoint.bssid == bssid)
    )
    row = result.first()
    if row is None:
        raise HTTPException(status_code=404, detail=f"No calibration for {bssid}")
    cal, bssid_val = row
    return CalibrationOut(
        bssid=bssid_val,
        band=cal.band,
        offset_m=cal.offset_m,
        std_dev_m=cal.std_dev_m,
        sample_count=cal.sample_count,
        is_reliable=cal.is_reliable,
    )


@router.delete("/calibration/{bssid}", status_code=status.HTTP_204_NO_CONTENT)
async def delete_calibration(
    bssid: str,
    db: AsyncSession = Depends(get_db),
    _auth: dict = Depends(_require_auth),
):
    result = await db.execute(select(AccessPoint).where(AccessPoint.bssid == bssid))
    ap = result.scalars().first()
    if ap is None:
        raise HTTPException(status_code=404, detail=f"No AP with bssid {bssid}")
    await db.execute(delete(ApCalibration).where(ApCalibration.ap_id == ap.ap_id))
    await db.commit()

--- backend/app/fusion/offset_corrector.py ---
﻿"""Offset correction — subtracts per-AP, per-band calibrated offset from raw RTT.

PRD §8.2:
    d_corrected = d_raw − d_offset(AP, band)
    clamp minimum: 0.5 m

Expected offsets: 2400–2700 m (5 GHz), ~1500 m (2.4 GHz legacy).
"""

_CLAMP_MIN_M: float = 0.5


def correct(d_raw_m: float, offset_m: float) -> float:
    """Return the corrected RTT distance, clamped to a minimum of 0.5 m.

    Args:
        d_raw_m:  Raw RTT distance in metres (straight from BLE packet).
        offset_m: Calibrated per-AP, per-band offset in metres.

    Returns:
        Corrected distance ≥ 0.5 m.
    """
    return max(d_raw_m - offset_m, _CLAMP_MIN_M)

--- backend/requirements.txt ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12
numpy==2.1.1
scikit-learn==1.5.2
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
pytest-cov==5.0.0

--- docker-compose.yml ---
services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: localization
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: changeme
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U admin -d localization" ]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql+asyncpg://admin:changeme@db:5432/localization
      SECRET_KEY: dev-secret-key-change-in-production
      GATEWAY_API_KEY: dev-gateway-key-change-in-production
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./backend/app:/app/app

volumes:
  postgres_data:


=== GIT LOG ===
c1e905f chore: add pytest.ini, conftest.py, and __init__.py chain for local test runs
b595249 [TASK-03,04,07,08,19] done: DB models, config/security, offset corrector, admin endpoints, health check — 45 tests pass
7634a5d [TASK-02] done: requirements.txt and Dockerfile — docker compose build passes
2e00f53 [TASK-01] done: full project scaffold — all directories and placeholder files created and pushed
35efe8d chore: initial project scaffold — all directories and placeholder files
