# ble_parser.py — retired.
#
# The BW16 binary packet parser (IMU type 0x01, RTT type 0x02) is no longer used.
# The XIAO ESP32-C5 firmware sends JSON directly; parsing is handled inline in
# backend/app/api/gateway.py.
#
# This file is kept as a placeholder to avoid import errors in any test stubs
# that may reference the module path.
